import { saveSession } from "./api";

export async function signInWithGoogle() {
  return { success: false, error: "Use email/password in this version" };
}
export async function signInWithApple() {
  return { success: false, error: "Use email/password in this version" };
}
